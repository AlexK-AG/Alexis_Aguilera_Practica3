# Build

Coloca en esta carpeta la exportación de Windows generada desde Godot, por ejemplo:

- `DodgeTheDemons.exe`
- `DodgeTheDemons.pck` (si la exportación lo genera por separado)

El ejecutable todavía está pendiente porque debe generarse con las plantillas de exportación instaladas en Godot.
